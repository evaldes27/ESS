services:
  proxy:
    image: nginx:alpine
    container_name: proxy-lae
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - lae-net

networks:
  lae-net:
    external: true

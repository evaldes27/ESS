services:
  portal:
    build: .
    container_name: lae-portal
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8011:8000"
    networks:
      - lae-net

networks:
  lae-net:
    external: true
